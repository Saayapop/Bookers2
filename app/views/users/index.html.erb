<h1>Users</h1>
<table border="1">
 <tr>
  <th>image</th>
  <th>name</th>
 </tr>
 <% @users.each do |user| %>
 <tr>
  <td><%= user.profile_image %></td>
  <td><%= user.name %></td>
  <td><%= link_to "Show", user_path(user.id) %></td>
 </tr>
 <% end %>
</table>